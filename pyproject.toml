[project]
name = "rich-logger"
version = "0.1.0"
description = "Rich-backed drop-in wrapper for loguru.logger"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "loguru>=0.7.3",
    "rich>=15.0.0",
    "rich-gradient>=0.3.13",
]
